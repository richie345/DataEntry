<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dataentry.ochanya.com.dataentry">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/smimage"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/smimage"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".User_Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SchoolRegistration" />
        <activity android:name=".MarketRegistration" />
        <activity android:name=".AdminMobileActivity" />
        <activity android:name=".AgentDashboard" />
        <activity android:name=".AgentReport" />
        <activity android:name=".SimpleReport" />
    </application>

</manifest>